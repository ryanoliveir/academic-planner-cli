

```
classDiagram
    class Travel {
        +string name
        +string date
        +Travel(string name, string date)
        +string getName() const
        +string getDate() const
    }

    class User {
        +string name
        +string email
        +User(string name, string email)
        +string getName() const
        +string getEmail() const
    }


```